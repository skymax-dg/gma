<%= render :partial => 'shared/page_title', :locals => {:record => @iva} %>
<%= render 'form' %>
<ul class="button-group [radius, rounded]">
  <li>
    <%= link_to 'Visualizza', @iva, :class => "button radius small" %>
  </li>
</ul>
