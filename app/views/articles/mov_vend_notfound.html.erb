<%= render :partial => 'shared/page_title', :locals => {:record => nil} %>
<div class="row">
  <div class="twelve columns">
    <fieldset>
      <div class="ten columns offset-by-one">
        <h4> Nessun movimento di <%= @tp == 'M' ? " Magazzino " : " Vendita " %> trovato </h4>
        <div class="row">
          <div class="twelve columns">
            <%= link_to 'Indietro', :back, :class => "button radius small" %>
          </div>
        </div>
      </div>
    </fieldset>
  </div>
</div>
