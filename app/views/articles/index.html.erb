<%= render :partial => 'shared/page_title', :locals => {:record => nil} %>
<div class="button-bar">
  <ul class="button-group">
    <li>
      <%= link_to 'NUOVO', new_article_path, :class => "button radius small new foundicon-add-doc" %>
    </li>
  </ul>
  <%= render :partial => 'btn_stp' %>
</div>
<%= render :partial => 'filter' %>
<% unless @articles.nil?||@articles.empty? %>
  <table>
    <thead>
      <tr>
        <th width="12%">Codice</th>
        <th width="6%">Categ.</th>
        <th width="44%">Descrizione</th>
        <th class="currency" width="10%">Prezzo</th>
        <th class="currency" width="10%">Costo</th>
        <th width="18%"></th>
      </tr>
    </thead>
    <tbody>
        <%= render :partial => 'line', :collection => @articles %>
    </tbody>
  </table>
  <%= will_paginate @articles %>
<% end %>
