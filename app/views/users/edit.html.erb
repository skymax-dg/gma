<%= render :partial => 'shared/main_title', :locals => {:t => @title, :record => @user} %>
<%= render 'form' %>
<div class="row">
  <div class="twelve columns">
    <ul class="button-group [radius, rounded]">
      <li>
       	<%= link_to 'Indietro', users_path, :class => "button radius small" %>
      </li>
    </ul>
  </div>
</div>
