<%= render :partial => 'shared/page_title', :locals => {:record => @user} %>
<%= render 'form' %>
<ul class="button-group [radius, rounded]">
  <li>
   	<%= link_to 'Indietro', users_path, :class => "button radius small" %>
  </li>
</ul>
