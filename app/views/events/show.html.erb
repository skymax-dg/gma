<%= render :partial => 'shared/page_title', :locals => {:record => nil} %>
<%= render "buttons" %>

<% if @article.evento? %>
  <%= render "show_course" %>

<% elsif @article.libro? %>
  <%= render "show_book" %>

<% elsif @article.rivista? %>
  <%= render "show_magazine" %>
<% end %>

