<%= render :partial => 'shared/main_title', :locals => {:t => @title, :record => @anaind} %>
<%= render 'form' %>
<div class="row">
  <div class="twelve columns">
    <ul class="button-group [radius, rounded]">
      <li>
        <%= link_to 'Visualizza', @anaind, :class => "button radius small" %>
      </li>
    </ul>
  </div>
</div>
