<div class="row">
  <div class="twelve columns">
    <fieldset>
      <legend> Ricerca citta' </legend>
      <%= form_tag(:filter_localitas, :method => :get) do %>
        <div class="three columns">
          <%= label :filter_localitas, "Ricerca per" %>
          <%= select_tag("tpfilter",
                         options_for_select([["Descrizione", "DE"], ["CAP", "CP"],
                                             ["Nazione", "NA"]], @tpfilter)) %>
        </div>
        <div class="five columns">
          <%= label :filter_localitas, "." %>
          <%= text_field_tag "desfilter", @desfilter %>
        </div>
        <div class="twelve columns">
          <%= submit_tag 'FILTRA', :class => "button radius small" %>
        </div>
      <% end %>
    </fieldset>
  </div>
</div>
<hr />
