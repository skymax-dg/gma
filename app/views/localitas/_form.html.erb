<%= render :partial => 'shared/error_messages', :locals => {:f => @localita} %>
<%= form_for(@localita) do |f| %>

  <div class="field">
    <b> <%= f.label "Descrizione" %> </b> <br />
    <%= f.text_field :descriz %>
  </div>
  <div class="field">
    <b> <%= f.label "Provincia" %> </b> <br />
    <%= f.text_field :prov %>
  </div>
  <div class="field">
    <b> <%= f.label "Cap" %> </b> <br />
    <%= f.text_field :cap %>
  </div>
  <div class="field">
    <b> <%= f.label "Stato/Nazione" %> </b> <br />
    <br />
    <%#= collection_select :localita, :paese_id, Paese.all, :id, :descriz, :prompt => true %>

<%= fields_for @localita.paese do |fp| %>
  <%= fp.text_field(:descriz, {:size => 50, :data => {:remote => true, :url => url_for(:controller => :localitas, :action => :ges_paese)}})%>
<% end %>

<div id="par_paese">
<%#= collection_select :localita, :paese_id, @paeses, :id, :descriz, {:include_blank => true}, {:hidden => true} %>
<%= render :partial => 'paese', :locals => {:hd => true} %>
</div>

  </div>
  <div class="field">
    <b> <%= f.label "CodFis" %> </b> <br />
    <%= f.text_field(:codfis, {:size => 5})%>
  </div>
  <div class="actions">
    <%= f.submit "SALVA"%>
  </div>
<% end %>
