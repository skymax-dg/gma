<%= render :partial => 'shared/page_title', :locals => {:t => @title, :record => @anagen} %>
<%= render 'form' %>
<div class="row">
  <div class="twelve columns">
    <ul class="button-group [radius, rounded]">
      <li>
        <%= link_to 'Visualizza', @anagen, :class => "button radius small" %>
      </li>
    </ul>
  </div>
</div>
