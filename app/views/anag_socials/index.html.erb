<h1>Listing socials</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @coupons.each do |coupon| %>
  <tr>
    <td><%= link_to 'Show', coupon %></td>
    <td><%= link_to 'Edit', edit_social_path(coupon) %></td>
    <td><%= link_to 'Destroy', social, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New social', new_social_path %>
